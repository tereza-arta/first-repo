pipeline {
    agent any
    environment {
        MY_VARIABLE = 'some_value'
    }
    stages {
        stage('Init stage') {
            steps {
                echo "Hello"
            }
        }
    }
}
